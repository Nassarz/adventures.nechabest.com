node_modules/
.next/
coverage/
.DS_Store
*.log
.env*
!.env.example

# clerk configuration (can include secrets)
/.clerk/

# Firebase config files contain API keys — do not commit
firebase-applet-config.json
firebase-blueprint.json

# Postman workspace files — not needed in repo
.postman.json
